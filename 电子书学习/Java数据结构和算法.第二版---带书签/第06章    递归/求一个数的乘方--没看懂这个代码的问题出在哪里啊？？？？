package 第06章_递归;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

/**
 * Title：XXXX OCR
 * Description:XXXX OCR 3.0
 * Copyright:Copyright(c) 2021
 * Company:XXXX 有限公司
 *
 * @author Wzi
 * @version jdk1.8
 * <p>
 * 带参数构造函数，初始化模式名、变量名称和数据源类型
 * @create_date 2019/10/4 15:09
 */
public class 求一个数的乘方 {
    public static void main(String[] args) throws IOException {
        System.out.println("获取一个数x，以及对应的pow：y");
        int x = Integer.parseInt(getString());
        System.out.println("输入的x是: "+x);
        int y = Integer.parseInt(getString());
        System.out.println("输入的y是:"+y);
        if (y%2==0){
            System.out.println(PowerCal(x,y));
        }else {
            System.out.println(PowerCal(x,y-1)*x);
        }
    }
    
    private static int PowerCal(int background, int exponent) {
        for (int i=exponent;i==1;i/=2){
            background*=background;
        }
        System.out.println("走到了这");
        return background;
    }
   

    public static String getString() throws IOException{
        InputStreamReader inputStreamReader = new InputStreamReader(System.in);
        BufferedReader bufferedReader = new BufferedReader(inputStreamReader);
        String str = bufferedReader.readLine();
        return str;
    }
}

