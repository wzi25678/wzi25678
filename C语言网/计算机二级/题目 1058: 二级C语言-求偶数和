#include<iostream>
using namespace std;
int main()
{
    int n,m,sum=0;
    cin>>n;
    for(int i=0;i<n;i++)
        {
            cin>>m;//输入数据
            if(m%2==0)//找到数据中的偶数
            sum+=m;//求和
        }
        cout<<sum<<endl;//输出偶数和
        return 0;
}
