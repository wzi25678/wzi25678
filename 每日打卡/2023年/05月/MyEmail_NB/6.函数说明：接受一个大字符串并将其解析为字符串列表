def textParse(bigString):      #将字符串转换成字符列表
    listOfTokens = re.split(r'\W*',bigString)   #将特殊字符号作为切分标志进行字符串切分，即非字母，非数字
    return [tok.lower() for tok in listOfTokens if len(tok) > 2]
    #除了首个字母外，其他单词编程小写
